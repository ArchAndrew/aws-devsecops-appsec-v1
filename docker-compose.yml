version: "3.9"

services:
  backend:
    build:
      context: ./app/backend
      dockerfile: Dockerfile
    container_name: devsecops-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
